# 高德地图集成配置说明

## 已完成的集成步骤

### 1. 添加依赖
已在 `app/build.gradle.kts` 中添加高德地图SDK依赖：
- 3D地图SDK
- 定位SDK
- 搜索SDK
- 导航SDK

### 2. 添加权限
已在 `AndroidManifest.xml` 中添加必要权限：
- 定位权限（ACCESS_FINE_LOCATION, ACCESS_COARSE_LOCATION）
- 网络权限
- 存储权限
- WiFi状态权限

### 3. 实现代码功能
- MainActivity中添加了定位权限请求
- MapSection组件集成了高德地图显示
- 实现了GPS实时导航功能
- 添加了导航信息实时显示

## 需要您完成的配置

### 获取高德地图API Key

1. **注册高德开放平台账号**
   - 访问：https://lbs.amap.com/
   - 注册并登录账号

2. **创建应用**
   - 进入控制台：https://console.amap.com/dev/key/app
   - 点击"创建新应用"
   - 填写应用名称：MyCarLauncher

3. **添加Key**
   - 在应用下点击"添加Key"
   - 服务平台选择：Android平台
   - 发布版安全码SHA1：运行以下命令获取
     ```bash
     keytool -list -v -keystore "%USERPROFILE%\.android\debug.keystore" -alias androiddebugkey -storepass android -keypass android
     ```
   - PackageName：`com.sephp.mycarlauncher`
   - 复制生成的Key

4. **配置API Key到项目**
   - 打开 `app/src/main/AndroidManifest.xml`
   - 找到以下行：
     ```xml
     <meta-data
         android:name="com.amap.api.v2.apikey"
         android:value="YOUR_AMAP_API_KEY" />
     ```
   - 将 `YOUR_AMAP_API_KEY` 替换为您获取的实际Key

### 同步项目

配置完成后，在Android Studio中：
1. 点击 "Sync Project with Gradle Files"
2. 等待依赖下载完成
3. 运行项目

## 功能说明

### 地图功能
- 实时定位显示
- 实时交通路况
- 地图缩放、平移

### 导航功能
- **启动导航**：点击地图区域开始导航
- **默认目的地**：北京天安门（可在代码中修改）
- **导航模式**：GPS实时导航
- **导航信息**：底部显示实时导航提示

### 自定义目的地

修改 `MainActivity.kt` 中 `MapSection` 函数的目的地坐标：
```kotlin
// 设置终点（当前为天安门）
endList.add(NaviLatLng(39.908823, 116.397470))
```

将坐标改为您需要的目的地经纬度即可。

## 注意事项

1. **权限授予**：首次运行时需要授予定位权限
2. **GPS信号**：在室内GPS信号较弱，建议在室外测试
3. **网络连接**：地图数据加载需要网络连接
4. **API配额**：注意高德地图免费配额限制

## 故障排查

如果遇到问题：
1. 检查API Key是否正确配置
2. 检查应用包名是否与高德平台配置一致
3. 检查SHA1签名是否正确
4. 查看Logcat中的错误信息（筛选 MapSection 标签）
